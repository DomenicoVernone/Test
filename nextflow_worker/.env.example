# .env.example — nextflow_worker
# Questo servizio non richiede un file .env.
# Tutte le variabili d'ambiente sono configurate nel docker-compose.yml
# tramite la sezione 'environment' del servizio nextflow_worker.