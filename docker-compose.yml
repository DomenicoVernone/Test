version: '3.8'

services:
  # Il nostro cervello clinico in R
  modello-r:
    build: ./microservizio_r
    ports:
      - "8080:8000"

  # Il backend in Python
  fastapi:
    build: ./fastapi
    ports:
      - "8000:8000"
    depends_on:
      - modello-r